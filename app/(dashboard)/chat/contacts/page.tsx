import { ContactsView } from '@/features/chat/components/contacts';

export const metadata = {
  title: 'Contactos del Chat',
};

export default function ContactsPage() {
  return <ContactsView />;
}
