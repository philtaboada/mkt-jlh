import { IntegrationsView } from '@/features/chat/components/settings';
export const metadata = {
  title: 'Integraciones del Chat',
};
export default function ChannelsPage() {
  return <IntegrationsView />;
}
